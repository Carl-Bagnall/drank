# Normalise to LF in the repository. CI runs on Linux; keeping the working
# tree LF too avoids spurious whole-file diffs between machines.
* text=auto eol=lf

# Binary assets must never be normalised.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
